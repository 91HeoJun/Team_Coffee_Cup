<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.mapper.MyPageMapper">


<!-- 마이페이지 회원 정보수정: 은주 -->


<!-- 로그인 AuthVO isLogin(LoginVO login) AuthVO:넘어갈 데이터/ LoginVO: 넘어온데이터-->
	<select id="selectByMember" resultType="com.company.domain.AuthVO">
	select userid, name from UserTBL where userid=#{userid} and password=#{password}
	</select>
	
	<!-- 회원탈퇴 -->
	<delete id="leaveMember">
	delete from UserTBL where userid=#{userid} and password=#{password}
	</delete>
	
	<!-- 비밀번호 변경 -->
	<update id="updateMember">
	update UserTBL set password=#{new_password} where userid=#{userid} and password=#{password} 
	</update>





</mapper>